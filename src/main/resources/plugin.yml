name: SRP
version: 0.0.1
author: Fx-Costa
main: com.fx.srp.SpeedRunPlus
api-version: 1.16
depend:
  - Multiverse-Core
  - Multiverse-NetherPortals

commands:
  srp:
    description: Main command for SpeedRunPlus.
    usage: /srp <gamemode> <action> [player]
    aliases: []
    tab-complete:
      - "<gamemode>"
      - "<action>"
      - "[player]"

permissions:
  # Admin commands
  srp.admin:
    description: Bypass any restrictions & access to admin tools
    default: false

  # General
  srp.use:
    description: Allows overall usage of /srp
    default: true

  srp.solo:
    description: Allows usage of the solo game mode
    default: true

  srp.battle:
    description: Allows usage of the battle game mode
    default: true

  srp.coop:
    description: Allows usage of the coop game mode
    default: true

  # Solo
  srp.solo.start:
    description: Allows starting a run
    default: true

  srp.solo.reset:
    description: Allows resetting a run
    default: true

  srp.solo.stop:
    description: Allows stopping a run
    default: true

  # Coop
  srp.coop.request:
    description: Allows requesting another player for a run
    default: true

  srp.coop.accept:
    description: Allows accepting a request for a run
    default: true

  srp.coop.decline:
    description: Allows declining a request for a run
    default: true

  srp.coop.stop:
    description: Allows stopping a run
    default: true

  # Battle
  srp.battle.request:
    description: Allows requesting another player for a run
    default: true

  srp.battle.accept:
    description: Allows accepting a request for a run
    default: true

  srp.battle.decline:
    description: Allows declining a request for a run
    default: true

  srp.battle.surrender:
    description: Allows surrendering a run
    default: true
